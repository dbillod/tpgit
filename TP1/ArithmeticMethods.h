bool IsOdd(int); // IsOdd checks if an integr is odd and returns true if the input integer is odd

bool IsPrime(unsigned int);// IsPrime : if input integer is prime, returns true. false otherwise

int Factorial(unsigned int);// Returns n! for any positive iunteger

int FactorialNoRecursion(unsigned int);// Same result as before but without using recursion

int GreatestPrimeFactor(unsigned int);// returns the greatest prime factor of any positive integer

void Pythagorician(void);// finds all the pythagoricain triplets until 1000